version: '3.3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${APP_PORT:-3005}:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
